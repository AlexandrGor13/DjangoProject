from app_orders.models import Order, Payment


def create_pyment(order: Order):
    # payment = Payment.objects.create(
    #     order=order,
    #     amount = order.total_amount,
    #
    # )
    pass
